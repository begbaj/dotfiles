#===============================
# Core functions
#===============================
#
# This file is prefixed by a 0 to be loaded before every other file
# Here are only functions that are used in other configurations that
# DO NOT rely on their content.
#
# If a function is needed and does rely on the content of one of the
# other configuration files, just declare the function there.

